global_requirements_incompatible: true
# Phase 1 is export-only: the nuScenes eval dataset / evaluator are not wired
# yet (get_evaluator / get_eval_dataset_classes are not overridden), so skip
# evaluate.py generation.
skip_evaluate: true
